name: Build

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'sbt'

    - name: Test
      run: sbt clean coverage test

    - name: Publish Site
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        folder: .

    - name: Coverage
      run: sbt coverageReport

    - name: Publish Coverage
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        folder: .
        target-folder: coverage

# @Todo: Add this and publish when https://github.com/sbt/sbt/issues/7065 resolved.
#    - name: Scaladoc
#      run: sbt doc
